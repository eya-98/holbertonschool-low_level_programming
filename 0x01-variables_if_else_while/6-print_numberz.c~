#include <stdio.h>
/**
 * main - prints out all numbers under 10
 * description: random and last digit
 * Return: Always 0
 */
int main(void)
{
char number;
for (number = '0'; number < '10'; number++)
putchar(number);
putchar("\n");
return (0);
}

